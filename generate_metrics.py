import pandas as pd
import numpy as np

# 1. Load your source matrix data
# (Assuming your dashboard data is saved in a CSV or structured directly in Python)
actuals = np.array([100, 120, 110, 130, 45, 50])
alpha = 0.40

# 2. Add your Demand Sensing Exponential Smoothing logic here...
# 3. Calculate metrics (MAE, RMSE, Bias)
# 4. Save the calculated data out to a JSON file or overwrite your HTML placeholders
